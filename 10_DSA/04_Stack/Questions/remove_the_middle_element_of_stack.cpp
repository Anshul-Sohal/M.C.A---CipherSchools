#include <iostream>
#include <stack>
using namespace std;

void removeMiddle(stack<int> st){
    if(st.top() == NULL){
        break;
    }
    
}

int main() {
    stack<int> st;
    st.push(10);
    st.push(20);
    st.push(30);
    removeMiddle(st);
    return 0;
}